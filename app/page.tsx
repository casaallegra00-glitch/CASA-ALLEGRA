'use client'

import { useMemo, useState } from 'react'
import { createClient } from '@supabase/supabase-js'

type Product={id:number;name:string;category:string;price:number;stock:number}
const PRODUCTS:Product[]=[{id:1,name:'Cuaderno personalizado',category:'Papelería',price:18500,stock:12},{id:2,name:'Planner semanal',category:'Organización',price:12900,stock:8},{id:3,name:'Kit stickers',category:'Regalos',price:6500,stock:24},{id:4,name:'Agenda 2026',category:'Papelería',price:22000,stock:5},{id:5,name:'Tarjetas personalizadas',category:'Gráfica',price:9800,stock:18},{id:6,name:'Combo emprendedor',category:'Gráfica',price:28500,stock:4}]
const money=(v:number)=>new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}).format(v)
const supabaseUrl=process.env.NEXT_PUBLIC_SUPABASE_URL
const supabaseKey=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
const supabase=supabaseUrl&&supabaseKey?createClient(supabaseUrl,supabaseKey):null

function passwordStrength(password:string){let score=0;if(password.length>=8)score++;if(/[a-z]/.test(password)&&/[A-Z]/.test(password))score++;if(/\d/.test(password))score++;if(/[^A-Za-z0-9]/.test(password))score++;if(password.length>=12)score++;return score<=1?'Baja':score<=3?'Media':'Alta'}

export default function Home(){
 const [search,setSearch]=useState('');const [cat,setCat]=useState('Todas');const [cart,setCart]=useState<Product[]>([]);const [tab,setTab]=useState('inicio');const [authMode,setAuthMode]=useState<'login'|'signup'|null>(null);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirmPassword,setConfirmPassword]=useState('');const [name,setName]=useState('');const [authMessage,setAuthMessage]=useState('');const [loading,setLoading]=useState(false);const [showPassword,setShowPassword]=useState(false);const [showConfirm,setShowConfirm]=useState(false)
 const cats=['Todas',...Array.from(new Set(PRODUCTS.map(p=>p.category)))];const visible=useMemo(()=>PRODUCTS.filter(p=>(cat==='Todas'||p.category===cat)&&p.name.toLowerCase().includes(search.toLowerCase())),[cat,search]);const total=cart.reduce((a,p)=>a+p.price,0)
 const openAuth=(mode:'login'|'signup')=>{setAuthMode(mode);setAuthMessage('');setEmail('');setPassword('');setConfirmPassword('');setName('');setShowPassword(false);setShowConfirm(false)}
 const submitAuth=async(e:React.FormEvent)=>{e.preventDefault();setAuthMessage('');if(!email||!password||(authMode==='signup'&&!name)){setAuthMessage('Completá todos los campos.');return}if(password.length<6){setAuthMessage('La contraseña debe tener al menos 6 caracteres.');return}if(authMode==='signup'&&password!==confirmPassword){setAuthMessage('Las contraseñas no coinciden.');return}if(!supabase){setAuthMessage('La conexión de cuenta no está configurada.');return}setLoading(true);try{if(authMode==='signup'){const {data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{full_name:name.trim()},emailRedirectTo:`${window.location.origin}/?confirmado=1`}});if(error)throw error;if(data.session){setAuthMode(null);setTab('panel')}else setAuthMessage('Cuenta creada. Revisá tu correo de CASA ALLEGRA APP para confirmar la cuenta.')}else{const {error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error;setAuthMode(null);setTab('panel')}}catch(error){setAuthMessage(error instanceof Error?error.message:'No pudimos completar la operación. Intentá nuevamente.')}finally{setLoading(false)}}
 return <div className="shell"><header className="top"><div className="brand"><span className="brand-mark">CA</span><span>CASA ALLEGRA APP</span></div><nav className="topnav"><button onClick={()=>setTab('inicio')}>Inicio</button><button onClick={()=>setTab('tienda')}>Tienda</button><button onClick={()=>setTab('panel')}>Mi negocio</button><button onClick={()=>openAuth('login')}>Ingresar</button><button onClick={()=>openAuth('signup')}>Crear cuenta</button><span className="chip">🛒 {cart.length}</span></nav></header>
 {tab==='inicio'||tab==='tienda'?<><section className="hero"><div className="hero-card"><span className="eyebrow">CASA ALLEGRA APP</span><h1>Tu tienda. Tus ventas. Todo en un solo lugar.</h1><p>Catálogo, carrito, clientes, pedidos, métricas, pagos y gestión comercial preparados para crecer.</p><div className="hero-actions"><button className="btn primary" onClick={()=>setTab('tienda')}>Ver tienda</button><button className="btn dark" onClick={()=>openAuth('signup')}>Crear mi cuenta</button></div></div><div className="side-card"><div><span className="eyebrow">Cuenta de comercio</span><h3>Creá tu tienda profesional</h3><p>Nombre, productos, usuarios y gestión desde PC o iPhone.</p></div><button className="btn primary" onClick={()=>openAuth('signup')}>Crear cuenta</button></div></section><section className="grid kpis"><div className="kpi"><span>Ventas del mes</span><strong>{money(428500)}</strong><small>↑ 18,4% vs. mes anterior</small></div><div className="kpi"><span>Ganancia estimada</span><strong>{money(126700)}</strong><small>Margen 29,6%</small></div><div className="kpi"><span>Pedidos activos</span><strong>14</strong><small>3 listos para despachar</small></div><div className="kpi"><span>Meta mensual</span><strong>74%</strong><small>Faltan {money(149000)}</small></div></section><section className="grid content"><div className="panel"><div className="section-title"><div><h2>Productos destacados</h2><div className="muted">Mostrá tus productos y recibí pedidos desde la tienda.</div></div><button className="btn" onClick={()=>setTab('tienda')}>Ver catálogo</button></div><div className="products">{PRODUCTS.slice(0,3).map(p=><article className="product" key={p.id}><div className="product-img">✦</div><h3>{p.name}</h3><p>{p.category} · Stock {p.stock}</p><div className="price-row"><span className="price">{money(p.price)}</span><button className="btn" onClick={()=>setCart(c=>[...c,p])}>Agregar</button></div></article>)}</div></div><aside className="panel"><h2>Centro de negocio</h2><div className="muted">Lo importante, resumido.</div><div className="list"><div className="row"><span>📊 Métricas</span><b>Día · semana · mes · año</b></div><div className="row"><span>💵 Rentabilidad</span><b>Ganancia / pérdida</b></div><div className="row"><span>🧮 Costos</span><b>Unidad · producto · lote</b></div><div className="row"><span>📱 Marketing</span><b>Redes + objetivos</b></div><div className="row"><span>🚚 Envíos</span><b>Mercado · Andreani · Correo</b></div></div></aside></section></>:<Panel tab={tab} total={total} cart={cart}/>} {tab==='tienda'&&<section className="grid"><div className="panel"><div className="toolbar"><input className="input" placeholder="Buscar productos..." value={search} onChange={e=>setSearch(e.target.value)}/>{cats.map(c=><button className="btn" key={c} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="products">{visible.map(p=><article className="product" key={p.id}><div className="product-img">✦</div><h3>{p.name}</h3><p>{p.category} · Stock {p.stock}</p><div className="price-row"><span className="price">{money(p.price)}</span><button className="btn primary" onClick={()=>setCart(c=>[...c,p])}>Agregar</button></div></article>)}</div></div><div className="panel"><h2>🛒 Tu carrito</h2><div className="list">{cart.map((p,i)=><div className="row" key={`${p.id}-${i}`}><span>{p.name}</span><b>{money(p.price)}</b></div>)}</div><div className="price-row" style={{marginTop:18}}><span>Total</span><strong className="price">{money(total)}</strong></div></div></section>}
 {authMode&&<div className="auth-overlay"><form className="panel auth-modal" onSubmit={submitAuth}><button type="button" className="btn auth-close" onClick={()=>setAuthMode(null)}>×</button><span className="eyebrow">CASA ALLEGRA APP</span><h2>{authMode==='signup'?'Crear cuenta':'Iniciar sesión'}</h2>{authMode==='signup'&&<input placeholder="Nombre del comercio" value={name} onChange={e=>setName(e.target.value)}/>}<input type="email" placeholder="Correo electrónico" value={email} onChange={e=>setEmail(e.target.value)} autoFocus/><div className="password-field"><input type={showPassword?'text':'password'} placeholder="Contraseña" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" className="eye" onClick={()=>setShowPassword(v=>!v)} aria-label={showPassword?'Ocultar contraseña':'Mostrar contraseña'}>{showPassword?'🙈':'👁️'}</button></div>{authMode==='signup'&&<><div className="password-field"><input type={showConfirm?'text':'password'} placeholder="Confirmar contraseña" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)}/><button type="button" className="eye" onClick={()=>setShowConfirm(v=>!v)}>{showConfirm?'🙈':'👁️'}</button></div><div className="strength">Seguridad de contraseña: <strong>{password?passwordStrength(password):'—'}</strong><div className="strength-bar"><span className={password?passwordStrength(password).toLowerCase():''}></span></div></div></>}<button type="submit" className="btn primary" disabled={loading}>{loading?'Procesando...':authMode==='signup'?'Crear cuenta':'Ingresar'}</button>{authMessage&&<div className="auth-message">{authMessage}</div>}<button type="button" className="btn" onClick={()=>openAuth(authMode==='signup'?'login':'signup')}>{authMode==='signup'?'Ya tengo una cuenta':'Crear una cuenta'}</button></form></div>}<footer className="footer">CASA ALLEGRA APP · gestión comercial y e-commerce.</footer></div>
}
function Panel({tab,total,cart}:{tab:string,total:number,cart:Product[]}){const sections=['inicio','ventas','productos','clientes','caja','pedidos','tienda','ia','reportes','integraciones','configuracion'];return <><section className="grid"><div className="panel"><span className="eyebrow">Panel del comercio</span><h1 style={{margin:'8px 0 4px'}}>CASA ALLEGRA</h1><p className="muted">Centro de gestión profesional · cada negocio tendrá sus propios datos y usuarios.</p><div className="hero-actions">{sections.slice(1).map(s=><button className="btn" key={s}>{s[0].toUpperCase()+s.slice(1)}</button>)}</div></div></section><section className="grid kpis"><div className="kpi"><span>Ventas</span><strong>{money(428500)}</strong><small>Este mes</small></div><div className="kpi"><span>Ganancia</span><strong>{money(126700)}</strong><small>Positiva</small></div><div className="kpi"><span>Carrito de prueba</span><strong>{cart.length}</strong><small>{money(total)}</small></div><div className="kpi"><span>Estado</span><strong>Activo</strong><small>Sincronización preparada</small></div></section></>}
