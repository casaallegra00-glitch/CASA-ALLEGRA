name: Deploy CASA ALLEGRA Web

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar proyecto
        uses: actions/checkout@v4

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Preparar versión web CASA ALLEGRA
        shell: bash
        run: |
          set -e
          test -f index.html
          test -f styles.css
          test -f app.js
          test -f casa-allegra-enhancements.js
          test -f casa-allegra-ai.js
          test -f casa-allegra-store.js
          test -f casa-allegra-pagos-envios.js
          test -f casa-allegra-pro.js
          test -f casa-allegra-multishop.js
          test -f casa-allegra-pro-dashboard.js
          test -f casa-allegra-analytics.js
          test -f integrations.js
          test -f dashboard-enhancements.js
          test -f dashboard-enhancements.css
          grep -q '</body>' index.html
          grep -q 'integrations.js' index.html || sed -i 's#</body>#<script src="integrations.js?v=3"></script>\n</body>#' index.html
          grep -q 'dashboard-enhancements.js' index.html || sed -i 's#</body>#<link rel="stylesheet" href="dashboard-enhancements.css?v=1">\n<script src="dashboard-enhancements.js?v=1"></script>\n</body>#' index.html
          grep -q 'dashboard-enhancements.js' index.html
          grep -q 'dashboard-enhancements.css' index.html
          touch .nojekyll

      - name: Crear artefacto de GitHub Pages
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publicar CASA ALLEGRA
        id: deployment
        uses: actions/deploy-pages@v4
